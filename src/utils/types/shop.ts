export type TypeShop = {
    _id: string,
    id_customer: string[],
    name: string,
    thumbnail: string,
    count_follower: number,
    star: number,
    description?: string,
    address?: string,
}